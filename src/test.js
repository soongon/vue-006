// javascript data type


// 기본 타입
const myStr = 'hello'; // str 타입
const myNumber = 12.3; // number 타입
const myBool = true;   // bool 타입 (true | false)

// 배열 타입 .. array
const myArray = [1, 2, 3, 4, 5];  // Array 타입

// 함수 타입 .. function 
const myFunc = function() { return 0; };  // Function
const myFunc2 = () => 0;

// Object 타입
const myObj = {
  a: 'a',
  b: 'b'
};